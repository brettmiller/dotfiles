# Setup fzf
# ---------
export FZF_DEFAULT_COMMAND="rg --files --no-ignore-vcs --hidden --glob '!.git/*'"
export FZF_TMUX_HEIGHT='50%'

if command -v brew && [[ ! "$PATH" == *$(brew --prefix)/opt/fzf/bin* ]]; then
  export PATH="${PATH:+${PATH}:}$(brew --prefix)/opt/fzf/bin"
fi

# Auto-completion
# ---------------
[[ $- == *i* ]] && command -v brew && source "$(brew --prefix)/opt/fzf/shell/completion.zsh" 2> /dev/null

# Key bindings
# ------------
if command -v brew &&  [[ -f $(brew --prefix)/opt/fzf/shell/key-bindings.zsh ]]; then
  source "$(brew --prefix)/opt/fzf/shell/key-bindings.zsh"
fi

